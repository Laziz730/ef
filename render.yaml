services:
  - type: web
    name: baklashga-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python baklashga.py
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: BOT_TOKEN
        sync: false